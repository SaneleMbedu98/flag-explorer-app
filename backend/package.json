{
  "name": "flag-explorer-backend",
  "version": "1.0.0",
  "description": "Backend for Flag Explorer App",
  "main": "app.js",
  "scripts": {
    "test": "jest",
    "clean": "rimraf dist",
    "build": "npm run clean && tsc && npm run copy-configs",
    "copy-configs": "",
    "minify": "terser dist/**/*.js --compress --mangle --output dist/"
  },
  "dependencies": {
    "node": ">=20.0.0",
    "axios": "^1.7.2",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "lodash": "^4.17.21",
    "mongoose": "^8.14.2",
    "nock": "^13.0.0"
  },
  "devDependencies": {
    "eslint": "^9.26.0",
    "jest": "^29.7.0",
    "rimraf": "^6.0.1",
    "supertest": "^6.3.3",
    "terser": "^5.39.0",
    "typescript": "^5.8.3"
  }
}
